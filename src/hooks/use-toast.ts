
// Import from the UI implementation
import { useToast, type Toast, type ToastActionElement } from "@/components/ui/toast";

// Re-export the toast components
export { useToast, type Toast, type ToastActionElement };

// Export toast function from sonner for consistency across the app
export { toast } from "sonner";
