
import React from "react";
import { toast } from "sonner";

// This is now a placeholder as the login logic has been moved to the Login page
const LoginForm = () => {
  React.useEffect(() => {
    console.log("LoginForm component is deprecated - login functionality moved to Login page");
  }, []);

  return null;
};

export default LoginForm;
